# Core
--extra-index-url https://download.pytorch.org/whl/cpu
torch
streamlit
numpy
pandas
sentence-transformers
chromadb
pypdf
python-dotenv

# LLM providers (only install/configure the ones you plan to use)
ollama          # local, free — requires `ollama serve` running and a model pulled

google-genai    # Gemini — requires GOOGLE_API_KEY
